import { SpecName } from './types.js';

interface ValidationOptions {
    recommendations?: boolean;
    warnings?: boolean;
}
interface ValidationOutput {
    critical?: Record<string, string> | string;
    errors?: ValidationError[];
    recommendations?: string[];
    valid: boolean;
    warnings?: string[];
}
interface ValidationError {
    field: string;
    message: string;
}
declare const validate: (data: object | string, specName?: SpecName, options?: ValidationOptions) => ValidationOutput;

export { type ValidationOptions, type ValidationOutput, validate };
